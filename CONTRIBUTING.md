# Contributing

```bash
npm install
npm run build
npm test
```

The build process compiles TypeScript and bundles dependencies into `dist/index.js` using [@vercel/ncc](https://github.com/vercel/ncc).
